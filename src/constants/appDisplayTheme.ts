/**
 * App shell theme — header, sidebar, canvas, and panel chrome (not the field diagram).
 *
 * Rollback: set to 'dark' to restore the original green app appearance.
 */
export type AppDisplayTheme = 'light' | 'dark'

export const APP_DISPLAY_THEME: AppDisplayTheme = 'light'

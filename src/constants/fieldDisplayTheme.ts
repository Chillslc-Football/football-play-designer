/**
 * Field display theme — controls turf, lines, player markers, and route colors only.
 *
 * Rollback: set to 'green' to restore the original green field appearance.
 */
export type FieldDisplayTheme = 'white' | 'green'

export const FIELD_DISPLAY_THEME: FieldDisplayTheme = 'white'
